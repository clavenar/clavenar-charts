apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "clavenar.fullname" . }}-outbound-resolution-pinning
  labels:
    {{- include "clavenar.labels" (dict "ctx" . "service" "outbound-resolution-pinning") | nindent 4 }}
immutable: true
data:
  outbound-resolution-pinning-v1.schema.json: |
{{ .Files.Get "files/outbound-resolution-pinning-v1.schema.json" | trimSuffix "\n" | indent 4 }}
  outbound-resolution-pinning-v1.fixture.json: |
{{ .Files.Get "files/outbound-resolution-pinning-v1.fixture.json" | trimSuffix "\n" | indent 4 }}
