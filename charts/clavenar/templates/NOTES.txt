Clavenar installed as release: {{ .Release.Name }}
Deployment profile: {{ .Values.deploymentProfile }}

{{- if eq .Values.deploymentProfile "production" }}

The production profile passed its fail-closed render gate: NetworkPolicy is
enabled; workload TLS comes from the existing Secret
`{{ .Values.tlsBundle.secretName }}` with auto-mint disabled; console operator
mTLS uses the separate public trust Secret
`{{ .Values.services.console.operatorMtls.publicTrustSecretName }}`; HIL auth
uses the existing Secret `{{ .Values.authSecrets.existingSecretName }}`; and
ledger accepts forwarded addresses only from
`{{ .Values.services.ledger.trustedProxySpiffe }}` over mTLS. The configured
website peer selector is admitted only to ledger port 8183 and names the
canonical website pod in an external namespace distinct from the release and
Prometheus namespaces.

This is a chart configuration gate, not production-readiness certification.
Validate the matching service images, release artifacts, external PKI and CNI,
persistence, backup and disaster recovery, and runtime behavior separately.
{{- end }}

The nine services live behind ClusterIP — by default not exposed
outside the cluster. Forward them locally:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ .Release.Name }}-proxy 8443:8443
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ .Release.Name }}-brain 8081:8081
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ .Release.Name }}-policy-engine 8082:8082
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ .Release.Name }}-ledger 8083:8083
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ .Release.Name }}-hil 8084:8084
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ .Release.Name }}-identity 8086:8086
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ .Release.Name }}-deep-review 8087:8087
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ .Release.Name }}-assurance 8088:8088

{{- if .Values.services.console.operatorMtls.enabled }}

Console operator mTLS is enabled on port 8085. TLS terminates in the
console process and a client must present a CA-valid certificate whose
fingerprint + SPIFFE URI exactly match an active operators.json entry:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ .Release.Name }}-console 8085:8085

Keep the operator CA private key and operator leaf private keys outside
the cluster. Forwarded client-certificate or role headers are ignored.
{{- if .Values.services.console.demo.enabled }}

The separate curated demo listener is enabled on plain HTTP port 9085:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ .Release.Name }}-console 9085:9085
{{- end }}
{{- else if eq .Values.services.console.auth.mode "webauthn" }}

Console passkey authentication is enabled on plain HTTP port 8085 for an
exact localhost port-forward. Browsers treat localhost as a secure WebAuthn
context; do not publish this HTTP listener on another hostname:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ .Release.Name }}-console 8085:8085

Open http://localhost:8085 after an Admin has consumed the deployment's
one-use bootstrap authority. The existing-cluster installer prints the exact
first-Admin setup URL without placing its token in the install receipt.
{{- else }}

Console is in the safe demo-only posture on plain HTTP port 8085. A
valid signed demo cookie is prefix-scoped and never grants an operator
session or Admin authority:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ .Release.Name }}-console 8085:8085
{{- end }}

Console diagnostics are container-only on plain HTTP port 9185. Kubelet
and Prometheus use this listener; inspect it locally through the
Deployment, not the application Service:

  kubectl -n {{ .Release.Namespace }} port-forward deploy/{{ .Release.Name }}-console 9185:9185
  curl localhost:9185/health
  curl localhost:9185/readyz

Assurance control on Service port 8088 requires a CA-valid client leaf with
the exact `spiffe://clavenar.local/service/console` URI. Its plain diagnostics
are container-only and expose no status or mutation routes:

  kubectl -n {{ .Release.Namespace }} port-forward deploy/{{ .Release.Name }}-assurance 9088:9088
  curl localhost:9088/health
  curl localhost:9088/readyz

{{- if .Values.tlsBundle.secretName }}

Brain Service port 8081 is the workload-mTLS application listener. Explain
requires the exact policy-engine identity; narrate/model-snapshot require the
exact console identity. Plain diagnostics are container-only and expose no
model or provider operation:

  kubectl -n {{ .Release.Namespace }} port-forward deploy/{{ .Release.Name }}-brain 9081:9081
  curl localhost:9081/health
  curl localhost:9081/readyz
{{- else }}

Brain keeps its no-bundle compatibility listener on pod loopback. The
published Service does not provide a plaintext auxiliary-provider path; supply
tlsBundle.secretName before expecting the Brain Service to become reachable.
{{- end }}

Health/readiness:

  curl localhost:8082/readyz   # policy-engine
  curl localhost:8083/readyz   # ledger
  curl localhost:8084/readyz   # hil
  curl localhost:8086/readyz   # identity
  curl localhost:8087/health   # deep-review

Proxy serves /mcp on its mTLS port (8443). /health, /readyz, and
/metrics are on a non-mTLS health listener (8080 by default,
container-port only — not published by the Service). Kubelet probes
target 8080. To exercise the mTLS path locally, point a client with
valid certs at https://localhost:8443/ per clavenar-proxy/README.md.

{{- if and (not .Values.tlsBundle.secretName) (not .Values.tlsBundle.autoMint) }}

WARNING: tlsBundle.secretName is empty.

The proxy and assurance control plane fail at boot without /certs mounted
(see their recurring-gotchas documentation). Three ways forward:

  1. Auto-mint inside the cluster (quickstart path):

       helm upgrade {{ .Release.Name }} . --reuse-values \
         --set tlsBundle.autoMint=true \
         --set tlsBundle.secretName=clavenar-certs

  2. Mint locally and project as a Secret:

       clavenar-proxy/scripts/gen_certs.sh --env prod
       kubectl -n {{ .Release.Namespace }} create secret generic clavenar-certs \
         --from-file=clavenar-proxy/certs/
       helm upgrade {{ .Release.Name }} . --reuse-values \
         --set tlsBundle.secretName=clavenar-certs

  3. Reuse an existing bundle Secret your PKI already publishes.
{{- end }}

{{- if and .Values.tlsBundle.autoMint .Values.tlsBundle.secretName }}

Auto-mint mode: a pre-install Job populates the Secret
`{{ .Release.Namespace }}/{{ .Values.tlsBundle.secretName }}` with a
self-signed CA + per-service workload certs. Use this for evaluation
clusters only — production deployments should ship a bundle from a
managed PKI (set `tlsBundle.autoMint=false` and provide
`tlsBundle.secretName` from an existing Secret).
Ordinary upgrades use rotation.operation=reconcile and preserve a valid
stable Secret exactly. A trust change requires operation=rotate, a new
rotation.generation, and reason=membership, expiry, dns, or profile; return operation to
reconcile/reason=none after the successful rotation release.
{{- end }}

{{- if .Values.nats.bundled.enabled }}

Bundled NATS: the upstream nats-io/nats subchart is installed as
`{{ .Release.Name }}-nats`. JetStream persistence (when enabled in
your values) lands on a PVC. `helm.url` is auto-overridden to
`nats://{{ .Release.Name }}-nats:4222` for in-stack reach.
{{- end }}

{{- if .Values.vault.bundled.enabled }}

Bundled Vault (DEV MODE): the upstream hashicorp/vault subchart is
installed at `{{ .Release.Name }}-vault` with a known root token
({{ .Values.vault.bundled.devToken }}) and no persistence. A
post-install hook Job enables the transit engine and creates the
Ed25519 `{{ .Values.vault.bundled.transit.keyName }}` key. Dev-mode Vault
loses state on pod restart; the hook re-runs on every chart upgrade
and re-provisions the key. Do not use this configuration for
production — flip `vault.bundled.enabled=false` and supply your own
Vault via `vault.addr` + `vault.tokenSecretName`, with distinct scoped keys
selected by `vault.identityTokenKey` and `vault.proxyTokenKey`.
{{- end }}

{{- if not .Values.networkPolicy.enabled }}

NOTE: NetworkPolicy is disabled. Spec §"Threat model" §"Cross-cutting"
treats k8s NetworkPolicy as the deployment perimeter. Enable when
your CNI supports it:

  helm upgrade {{ .Release.Name }} . --reuse-values \
    --set networkPolicy.enabled=true
{{- end }}

Chart layout: 9 services, 3 PVCs (ledger / hil / identity SQLite mode),
default-on NetworkPolicy + replica-aware PDBs. See
charts/clavenar/README.md for the full values reference.
