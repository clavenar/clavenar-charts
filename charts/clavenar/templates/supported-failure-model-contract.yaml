apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "clavenar.fullname" . }}-supported-failure-model
  labels:
    {{- include "clavenar.labels" (dict "ctx" . "service" "supported-failure-model") | nindent 4 }}
immutable: true
data:
  supported-failure-model-v1.schema.json: |
{{ .Files.Get "files/supported-failure-model-v1.schema.json" | trimSuffix "\n" | indent 4 }}
  supported-failure-model-v1.fixture.json: |
{{ .Files.Get "files/supported-failure-model-v1.fixture.json" | trimSuffix "\n" | indent 4 }}
