apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "clavenar.fullname" . }}-rooted-path-target-validation
  labels:
    {{- include "clavenar.labels" (dict "ctx" . "service" "rooted-path-target-validation") | nindent 4 }}
immutable: true
data:
  rooted-path-target-validation-v1.schema.json: |
{{ .Files.Get "files/rooted-path-target-validation-v1.schema.json" | trimSuffix "\n" | indent 4 }}
  rooted-path-target-validation-v1.fixture.json: |
{{ .Files.Get "files/rooted-path-target-validation-v1.fixture.json" | trimSuffix "\n" | indent 4 }}
